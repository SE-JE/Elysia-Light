import { {{ model }} } from "@models";

export default async function {{ name }}Seeder() {
    // =========================>
    // ## Seed the application's database
    // =========================>
    
    await {{ name }}.query().insert([
      {{ seeders }}
    ]);
}
